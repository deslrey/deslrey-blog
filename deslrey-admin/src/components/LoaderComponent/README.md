# LoaderComponent 组件

## 功能

LoaderComponent 组件是一个加载状态指示器，用于在数据加载过程中显示加载动画。

## 使用方法

```tsx
import LoaderComponent from './LoaderComponent';

function MyComponent() {
  return (
    <LoaderComponent />
  );
}
```
